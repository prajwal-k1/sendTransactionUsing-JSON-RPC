# sendTransactionUsing-JSON-RPC

I wanted to experiment and see how transaction creation happens, how can we send transaction under the hood so figured I could work on it and see, this is the result.
